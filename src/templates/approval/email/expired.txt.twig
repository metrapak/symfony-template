Hello {{ recipientName }},

"{{ purchaseDescription }}" for {{ childName }} expired after 48 hours with no answer, so it was
denied automatically. Nothing has been charged.

If {{ childName }} still wants it, they can ask again.

Hello {{ recipientName }},

This is for your information - there is nothing for you to approve.

{{ childName }} bought "{{ purchaseDescription }}" for {{ spokenAmount }} using tokens. You allow
this child to spend tokens without approval, so they were registered straight away.

Change what {{ childName }} may spend without asking: {{ settingsUrl }}
